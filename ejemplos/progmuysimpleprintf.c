#include <stdio.h>
#define PI 3.1416

/* Este es el primer programa 
   para ver el funcionamiento del printf */
main () {
  int v,v2;
  v=33;  // le damos un valor inicial a la v
  v2=22;
  printf ("valor de v=%d, valor de v2 %d \n",v, v2);
  printf ("valor de v=, valor de v2 %d \n",v, v2);

}
